import { DelegatedUsersRepository } from "../../sequelize/DelegatedUsersRepository";

export const delegatedUsersRepository = new DelegatedUsersRepository()

describe("Test DelegatedUserService Methods", () => {
  describe('some test', () => {
    it('should a test', function () {

    });
  });
})
